#events-list(ng-controller="EventsCtrl")
  %google-map(center="map.center" zoom="map.zoom" draggable="true" options="map.options")
    %markers(models="map.markers" coords="'self'" icon="'icon'" click="'onClicked'")


